export class Category {
    constructor (
        public id?: number,
        public name?: string,
        public moment?: string,
        public imageUrl?: string,
        public description?: string
    ) {}
}